EXPO_PUBLIC_API_URL=http://localhost:8000
# Clerk Dashboard → API Keys → Publishable key. This is safe to expose to the app.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_with_your_clerk_publishable_key

# Clerk Dashboard → Native applications: register com.tether.app for iOS and Android.
# The native callback used by hosted email authentication is com.tether.app://callback.
